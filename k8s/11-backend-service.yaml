apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: socialflow
  labels:
    app: backend
spec:
  clusterIP: None
  selector:
    app: backend
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: backend